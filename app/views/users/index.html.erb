<div class="wrapper">
   <h3>List of All Current Users:</h3>
   <div class="user-list">
      <table>
      <tr>
        <th>Username</th>
      </tr>
      <% @users.each do |user| %>
        <tr>
          <td><%=user.username %></td>
        	<td><%= link_to "Show", user_path(user) %></td>
        	<td><%= link_to "Edit", edit_user_path(user) %></td>
        	<td><%= link_to "Delete", user_path(user) %></td>
        </tr>
      <% end %>
      </table>
    </div>
</div>

    
